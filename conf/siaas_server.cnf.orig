# Protected configurations (last for the whole run and can't be changed remotely)

log_level = info
mongo_collection = siaas
mongo_db = siaas
mongo_host = 127.0.0.1
mongo_port = 27017
mongo_pwd = siaas
mongo_user = siaas

# Runtime configurations (can be changed during runtime by the remote server)

dbmaintenance_history_days_to_keep = 365
dbmaintenance_loop_interval_sec = 86400
mailer_loop_interval_sec = 86400
mailer_smtp_account = siaas.iscte@gmail.com
mailer_smtp_pwd = password123
#mailer_smtp_receivers = siaas.iscte@gmail.com # comment this line to not send anything
mailer_smtp_server = smtp.gmail.com
mailer_smtp_tls_port = 587
mailer_smtp_report_type = exploit_vuln_only # all, vuln_only (default), exploit_vuln_only
platform_loop_interval_sec = 300
